const APP_HOST = 'localhost';
const APP_PORT = 3000;

const API_HOST = 'localhost';
const API_PORT = 8811;

const CSRF_COOKIE_NAME = '__csrf_token';
const SESSION_COOKIE_NAME = '__the_videohunt_session';

module.exports = {
    APP_HOST, APP_PORT, API_HOST, API_PORT,
    CSRF_COOKIE_NAME, SESSION_COOKIE_NAME
}
